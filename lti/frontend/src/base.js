import React from 'react';

const Base = () => {
  return (
    <div>
      Base
    </div>
  );
};

export default Base;

// i hate recreating shit so this makes life easier, will delete when project is over